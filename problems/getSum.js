// the other questions were about cloning (examples were primitives)
// and an easy one using console logs and assigning values

function getSum(endpoints) {
    var sum = 0;
    var returns = 0;
    // ...
    
    endpoints.forEach(function(val, idx, arr) {
      $.get(val, function(d) {
      })
      .then(function(d) {
        sum += logins;
        returns += 1;
        if (returns === endpoints.length) {
          return sum;
        }
    });
}
 
$.get('/serviceA?user=foo', function(d) {
    // ...
});
